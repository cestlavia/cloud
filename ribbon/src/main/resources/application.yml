spring:
  application:
    name: ribbon

server:
  port: 8804
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8801/eureka/
#    fetch-registry: true #默认值true
#ribbon:
#  http:
#    client:
#      enabled: true
#  eureka:
#    enabled: true

